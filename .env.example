# Example environment variables (do not include secrets in repository)

# Backend / Database Configuration
DATABASE_URL=postgres://postgres:example@localhost:5432/nrsgirls
JWT_SECRET=replace-me-with-a-secure-secret

# S3 / Object Storage Configuration
S3_ENDPOINT=https://s3.example.com
S3_ACCESS_KEY=replace-me
S3_SECRET_KEY=replace-me

# Frontend / Stripe Configuration (for frontend/nextjs)
# Copy this file to frontend/nextjs/.env.local and fill in your actual values
# Stripe API Keys (get from https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# For local development with Stripe CLI:
# 1. Install Stripe CLI: https://stripe.com/docs/stripe-cli
# 2. Run: stripe listen --forward-to localhost:3000/api/webhook
# 3. Copy the webhook secret from CLI output to STRIPE_WEBHOOK_SECRET above

